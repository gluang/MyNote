# 🔥 获取 master 最新一次的 id



$ git checkout docs
# 拉取 main 最新一次的提交
$ git cherry-pick [id]
$ make server

$ git add .
$ git commit -m "xxx"
$ git push origin docs

# 切回 main 
$ git checkout main


# 🔥 备份
zip 
